<% @notifications.where('created_at >= ?', Time.now).each do |notification| %>
court id <%= notification.notifiable.court.id %>,
notif id
<%= notification.id %>,
notif created at <%= notification.created_at %>,
court datetime <%= notification.notifiable.court.date.to_datetime %>,
time now <%= Time.now %><br>
time zone <%= Time.now.in_time_zone %>
<% end %>

<ul class="list-group col-md-4 col-md-offset-4">
  <li class="list-group-item">
    <h1 class="text-center"><small>Vos notifications</small></h1>
  </li>

  <% @notifications.each do |notification| %>
    <% unless current_user == notification.notifier %>
      <% unless notification.message.include? "invite" %>
      <li class="list-group-item">
        <div class="row">
          <div class="col-md-2">
            <%= link_to image_tag(notification.notifier.avatar.url(:mini), class: "img-rounded"), user_path(notification.notifier) %>
          </div>
          <div class="col-md-10">

            <%= render :partial => "notifiable_message", :locals => {:notification => notification} %>
            <div class="text-right"><small><%= l notification.created_at, :format => ('%e %b à %H:%M') %></small></div>
          </div>
        </div>
      </li>
      <% else %>
        <% if notification.notifiable.court.past %>
          <li class="list-group-item">
            <div class="row">
              <div class="col-md-2">
                <%= link_to image_tag(notification.notifier.avatar.url(:mini), class: "img-rounded"), user_path(notification.notifier) %>
              </div>
              <div class="col-md-10">
                <%= link_to notification.message, court_path(notification.notifiable.court.id) %>
                <div class="text-right"><small><%= l notification.created_at, :format => ('%e %b à %H:%M') %></small></div>
              </div>
            </div>
          </li>
        <% end %>
      <% end %>
    <% end %>
  <% end %>
</ul>
<%= form_for(@prefered_area) do |f| %>
  <% if @prefered_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prefered_area.errors.count, "error") %> prohibited this prefered_area from being saved:</h2>

      <ul>
      <% @prefered_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
